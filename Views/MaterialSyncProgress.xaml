<Window x:Class="Miller_Craft_Tools.Views.MaterialSyncProgress"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Material Sync Progress"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <StackPanel Margin="10">
        <TextBlock x:Name="StatusText" Text="Starting…" Margin="0,0,0,10"/>
        <ProgressBar x:Name="ProgressBar" Height="20" Minimum="0" Maximum="100"/>
        <Button x:Name="CancelButton"
                Content="Cancel"
                Width="80"
                Margin="0,10,0,0"
                HorizontalAlignment="Right"/>
    </StackPanel>
</Window>
